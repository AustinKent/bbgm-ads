{
  "name": "bbgm-ads",
  "version": "0.0.1",
  "license": "MIT",
  "dependencies": {
    "prebid.js": "^0.34.5"
  },
  "devDependencies": {
    "ava": "^0.25.0",
    "babel-core": "^6.26.0",
    "babel-eslint": "^8.2.2",
    "babel-plugin-external-helpers": "^6.22.0",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^4.18.2",
    "eslint-config-prettier": "^2.9.0",
    "prettier": "^1.11.1",
    "rollup": "^0.56.4",
    "rollup-plugin-babel": "^3.0.3",
    "rollup-plugin-replace": "^2.0.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && node scripts/build.js",
    "lint": "eslint 'scripts/**/*.js' 'src/**/*.js' 'test/**/*.js'",
    "prettier": "prettier --write 'scripts/**/*.js' 'src/**/*.js' 'test/**/*.js'",
    "test": "yarn run lint && yarn run build test && ava",
    "update-prebid": "bash scripts/update-prebid.sh"
  }
}
